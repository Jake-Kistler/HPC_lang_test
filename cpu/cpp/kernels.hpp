#pragma once
#include <vector>

// this is what is defined in baseline.cpp
void baseline(const std::vector<float> &A,
            const std::vector<float> &B,
            std::vector<float> &C,
            size_t N);